html 
    head 
        title Javascript MQTT Websockets example
        script(src="https://cdnjs.cloudflare.com/ajax/libs/paho-mqtt/1.0.1/mqttws31.min.js" type="text/javascript")
        script(src="http://localhost:3000/static/mqttTest.js")
    body(onload="init()")
        h1 Main Body
        button(onclick="publishMessage()") publish message
        p#messageDisplay no message received yet